# Authentication

## jwt

- access token
  - contains user info
  - cosists of:
    - header
    - payload
    - signature
  - delivered in the request header
- refresh token
  - delivered in the POST data
